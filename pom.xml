<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<groupId>org.simulatest</groupId>
	<artifactId>restmock</artifactId>
	<version>0.2.0</version>
	<name>Rest Mock</name>
	<description>The simplest way to mock HTTP in JVM</description>
	<url>https://github.com/francisrangel/rest-mock</url>

	<licenses>
		<license>
			<name>The Apache Software License, Version 2.0</name>
			<url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
			<distribution>repo</distribution>
		</license>
	</licenses>

	<scm>
		<connection>scm:git:https://github.com/francisrangel/rest-mock.git</connection>
		<developerConnection>scm:git:git@github.com:francisrangel/rest-mock.git</developerConnection>
		<url>https://github.com/francisrangel/rest-mock</url>
	</scm>

	<developers>
		<developer>
			<id>gb</id>
			<name>Gabriel Such</name>
			<email>gabrielsuch@gmail.com</email>
		</developer>
		<developer>
			<id>francisrangel</id>
			<name>Francis Rangel</name>
			<email>francis.rangel@gmail.com</email>
		</developer>
	</developers>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
		<!-- Reproducible builds: same source bytes in, same jar bytes out.
		     Bump this to the release date on every release. -->
		<project.build.outputTimestamp>2026-09-02T00:00:00Z</project.build.outputTimestamp>
		<!-- release, not source/target: a build on a newer JDK then cannot
		     compile against an API that is missing on 17. -->
		<maven.compiler.release>17</maven.compiler.release>
		<junit.version>6.1.3</junit.version>
		<!-- slf4j-api is the only third-party jar pushed onto downstream
		     classpaths (compile scope). Pin to the last 1.7.x so Maven's
		     nearest-wins doesn't yank older Spring Boot 2.x users off
		     their 1.7-compatible logback. SLF4J's Logger/LoggerFactory
		     surface is wire-compatible across 1.7 and 2.x. -->
		<slf4j.version>1.7.36</slf4j.version>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-api</artifactId>
			<version>${slf4j.version}</version>
		</dependency>

		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-simple</artifactId>
			<version>${slf4j.version}</version>
			<scope>test</scope>
		</dependency>

		<!-- Required, not optional: thenReturnJSON/XML(Object) and the request
		     body flattening are core features, so a NoClassDefFoundError at
		     runtime would be the worse trade. Only long-stable Jackson 2 APIs
		     are used (readTree, writeValueAsString, findAndRegisterModules), so
		     a project that pins its own 2.x wins by nearest-wins and still
		     works. -->
		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-databind</artifactId>
			<version>2.21.3</version>
		</dependency>

		<dependency>
			<groupId>com.fasterxml.jackson.dataformat</groupId>
			<artifactId>jackson-dataformat-xml</artifactId>
			<version>2.21.3</version>
		</dependency>

		<!-- Optional: only RestMockExtension needs it, and anyone using that
		     class already has JUnit on their own test classpath. Without this
		     a TestNG or JUnit 4 project depending on rest-mock would be handed
		     the whole JUnit 5 API it never asked for. -->
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter-api</artifactId>
			<version>${junit.version}</version>
			<optional>true</optional>
		</dependency>

		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter-engine</artifactId>
			<version>${junit.version}</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<!-- Automatic-Module-Name: pins the module name for anyone on the
			     module path. Without it the name is derived from the jar
			     filename and shifts if the artifact is ever renamed. -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>3.5.1</version>
				<configuration>
					<archive>
						<manifestEntries>
							<Automatic-Module-Name>org.simulatest.restmock</Automatic-Module-Name>
						</manifestEntries>
					</archive>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.15.0</version>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>3.5.6</version>
			</plugin>
			<!-- Sources JAR: attached to every build so `mvn install` produces a
			     complete artifact set and Maven Central gets *-sources.jar. -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-source-plugin</artifactId>
				<version>3.4.0</version>
				<executions>
					<execution>
						<id>attach-sources</id>
						<phase>package</phase>
						<goals>
							<goal>jar-no-fork</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<!-- Javadoc JAR + lint: attached to every build so Maven Central gets
			     *-javadoc.jar. doclint catches HTML/reference/syntax/accessibility
			     issues but not missing comments - we document intent, not every getter. -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-javadoc-plugin</artifactId>
				<version>3.12.0</version>
				<configuration>
					<doclint>all,-missing</doclint>
					<quiet>true</quiet>
					<failOnWarnings>true</failOnWarnings>
					<!-- internal.* is documented as unstable and is enforced out of
					     every public signature by PublicSurfaceTest, so publishing
					     its API docs to Central would only invite use. Excluding it
					     also settles a javadoc error: the package exists to hold a
					     package-info and nothing else, which javadoc rejects once an
					     explicit maven-jar-plugin puts it in package-list mode. -->
					<excludePackageNames>org.simulatest.restmock.internal:org.simulatest.restmock.internal.*</excludePackageNames>
				</configuration>
				<executions>
					<execution>
						<id>attach-javadocs</id>
						<phase>package</phase>
						<goals>
							<goal>jar</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<!-- CycloneDX SBOM: emits a bom.xml + bom.json next to every jar so
			     downstream supply-chain scanners can enumerate dependencies
			     without re-resolving from Central. -->
			<plugin>
				<groupId>org.cyclonedx</groupId>
				<artifactId>cyclonedx-maven-plugin</artifactId>
				<version>2.9.3</version>
				<executions>
					<execution>
						<id>build-sbom</id>
						<phase>package</phase>
						<goals>
							<goal>makeBom</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<!-- Release-only concerns: GPG signing and Maven Central publishing.
		     Source and javadoc JARs are produced by the default build. -->
		<profile>
			<id>release</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-gpg-plugin</artifactId>
						<version>3.2.8</version>
						<configuration>
							<!-- Loopback pinentry: read the passphrase from the
							     MAVEN_GPG_PASSPHRASE env var (or -Dgpg.passphrase)
							     instead of opening a GUI prompt. Works the same
							     locally and in CI. -->
							<gpgArguments>
								<arg>--pinentry-mode</arg>
								<arg>loopback</arg>
							</gpgArguments>
						</configuration>
						<executions>
							<execution>
								<id>sign-artifacts</id>
								<phase>verify</phase>
								<goals>
									<goal>sign</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
					<plugin>
						<groupId>org.sonatype.central</groupId>
						<artifactId>central-publishing-maven-plugin</artifactId>
						<version>0.11.0</version>
						<extensions>true</extensions>
						<configuration>
							<publishingServerId>central</publishingServerId>
							<autoPublish>true</autoPublish>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

</project>
